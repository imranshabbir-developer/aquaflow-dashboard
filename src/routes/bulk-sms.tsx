import { createFileRoute } from '@tanstack/react-router'; import { AppShell } from '@/components/app/app-shell'; import { BulkSmsPage } from '@/components/app/management-pages';
export const Route=createFileRoute('/bulk-sms')({head:()=>({meta:[{title:'Bulk SMS — P3 Care'},{name:'description',content:'Create and schedule SMS campaigns.'},{property:'og:title',content:'Bulk SMS — P3 Care'},{property:'og:description',content:'Create and schedule SMS campaigns.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:()=> <AppShell><BulkSmsPage/></AppShell>});
