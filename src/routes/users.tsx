import { createFileRoute } from '@tanstack/react-router'; import { AppShell } from '@/components/app/app-shell'; import { UsersPage } from '@/components/app/management-pages';
export const Route=createFileRoute('/users')({head:()=>({meta:[{title:'Add / Edit User — P3 Care'},{name:'description',content:'Create and manage workspace users.'},{property:'og:title',content:'Add / Edit User — P3 Care'},{property:'og:description',content:'Create and manage workspace users.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:()=> <AppShell><UsersPage/></AppShell>});
