import { createFileRoute } from '@tanstack/react-router'; import { AppShell } from '@/components/app/app-shell'; import { SettingsPage } from '@/components/app/management-pages';
export const Route=createFileRoute('/settings')({head:()=>({meta:[{title:'Settings — P3 Care'},{name:'description',content:'Configure messaging workspace settings.'},{property:'og:title',content:'Settings — P3 Care'},{property:'og:description',content:'Configure messaging workspace settings.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:()=> <AppShell><SettingsPage/></AppShell>});
