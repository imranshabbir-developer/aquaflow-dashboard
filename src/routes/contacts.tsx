import { createFileRoute } from '@tanstack/react-router'; import { AppShell } from '@/components/app/app-shell'; import { ContactsPage } from '@/components/app/management-pages';
export const Route=createFileRoute('/contacts')({head:()=>({meta:[{title:'Contacts — P3 Care'},{name:'description',content:'Manage contacts and communication details.'},{property:'og:title',content:'Contacts — P3 Care'},{property:'og:description',content:'Manage contacts and communication details.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:()=> <AppShell><ContactsPage/></AppShell>});
